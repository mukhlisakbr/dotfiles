# ohmyzsh
export ZSH="$HOME/.oh-my-zsh"
ZSH_THEME="robbyrussell"
zstyle ':omz:plugins:eza' 'icons' yes
zstyle ':omz:plugins:eza' 'dirs-first' yes
plugins=(
	git
	npm
	brew
	composer
	nvm
	ssh
	sublime
	laravel
	eza
	copypath
	fzf
	zoxide
)
source "$ZSH/oh-my-zsh.sh"

# zinit
source /opt/homebrew/opt/zinit/zinit.zsh
zinit light zsh-users/zsh-autosuggestions
zinit light zsh-users/zsh-syntax-highlighting

# homebrew
export HOMEBREW_NO_ENV_HINTS=1

# environment
export EDITOR='vim'

# bun
export BUN_INSTALL="$HOME/.bun"
export PATH="$BUN_INSTALL/bin:$PATH"

# yazi
function y() {
	local tmp="$(mktemp -t "yazi-cwd.XXXXXX")" cwd
	command yazi "$@" --cwd-file="$tmp"
	IFS= read -r -d '' cwd < "$tmp"
	[ "$cwd" != "$PWD" ] && [ -d "$cwd" ] && builtin cd -- "$cwd"
	rm -f -- "$tmp"
}

# git
alias wip='git add . && git commit -m "wip" && git push'

# claudecode
alias cc="claude"
alias ccd="claude --dangerously-skip-permissions"
alias ccc="claude -c"

# opencode
alias oc="opencode"

# codex
alias co="codex"

# uv
export PATH="/Users/batman/.local/bin:$PATH"

# java + android
export JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-17.jdk/Contents/Home
export ANDROID_HOME="$HOME/Library/Android/sdk"
export PATH="$PATH:$ANDROID_HOME/emulator"
export PATH="$PATH:$ANDROID_HOME/platform-tools"
export PATH="$PATH:$ANDROID_HOME/cmdline-tools/latest/bin"

# herd
export PATH="/Users/batman/Library/Application Support/Herd/bin:$PATH"
export HERD_PHP_84_INI_SCAN_DIR="/Users/batman/Library/Application Support/Herd/config/php/84/"
export HERD_PHP_85_INI_SCAN_DIR="/Users/batman/Library/Application Support/Herd/config/php/85/"
export NVM_DIR="/Users/batman/Library/Application Support/Herd/config/nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
[[ -f "/Applications/Herd.app/Contents/Resources/config/shell/zshrc.zsh" ]] && builtin source "/Applications/Herd.app/Contents/Resources/config/shell/zshrc.zsh"

# bitwarden ssh agent
export SSH_AUTH_SOCK=/Users/batman/Library/Containers/com.bitwarden.desktop/Data/.bitwarden-ssh-agent.sock

# test
